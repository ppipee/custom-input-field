{
  "type": "component",
  "metadata": {},
  "schema": {
    "name": "custom-input-field",
    "friendlyName": "Custom Input Field",
    "description": "the custom input field, which allows you to select any data type you want.",
    "icon": "Text",
    "settings": [
      {
        "type": "text",
        "key": "field",
        "label": "Field"
      },
      {
        "type": "text",
        "key": "label",
        "label": "Label"
      },
      {
        "type": "text",
        "key": "placeholder",
        "label": "Placeholder"
      },
      {
        "type": "text",
        "key": "defaultValue",
        "label": "Default Value"
      },
      {
        "type": "select",
        "key": "type",
        "label": "type",
        "options": ["text", "number", "boolean", "array", "json", "datetime"]
      },

      {
        "type": "event",
        "key": "onChange",
        "label": "onChange"
      },
      {
        "type": "boolean",
        "key": "disabled",
        "label": "disabled"
      },
      {
        "type": "validation/string",
        "key": "validation",
        "label": "Validation"
      },
      {
        "type": "select",
        "key": "align",
        "label": "align",
        "options": ["left", "center", "right"]
      }
    ]
  }
}
